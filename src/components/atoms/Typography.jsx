import styled from 'styled-components';

const PrimaryTitle = styled.h1`
	font-size: 24px;
	color: var(--text-grey);
`;
const FaintText = styled.p`
	color: var(--text-grey);
	font-size: 12px;
`;
const BoldText = styled.p`
	font-weight: bold;
	font-size: 1.05rem;
`;
const ThinText = styled.p`
	font-weight: 200;
`;

export { PrimaryTitle, FaintText, BoldText, ThinText };
